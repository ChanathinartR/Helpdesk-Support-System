services:
    db:
        image: postgres:16
        restart: always
        volumes:
            - pgdata:/var/lib/postgresql/data
        environment:
            POSTGRES_PASSWORD: P@ssw0rd
            POSTGRES_USER: postgres
            POSTGRES_DB: hd
        ports:
            - "5432:5432"

volumes:
    pgdata:
